import 'package:flutter/material.dart';

class PortfolioTabletLayout extends StatelessWidget {
  const PortfolioTabletLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}